name: Create Packet PR from Issue

on:
  issues:
    types: [opened]

jobs:
  create-packet-pr:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Parse issue and create packet
        uses: actions/github-script@v7
        with:
          script: |
            const issue = context.payload.issue;
            const body = issue.body || '';

            // Parse GitHub Issue Form fields (### Label\n\nValue format)
            function parseField(label) {
              const regex = new RegExp(`### ${label}\\s*\\n\\n([\\s\\S]*?)(?=\\n### |$)`);
              const match = body.match(regex);
              return match ? match[1].trim() : '';
            }

            function parseCSV(label) {
              const val = parseField(label);
              return val ? val.split(',').map(s => s.trim()).filter(Boolean) : [];
            }

            const title = parseField('Lab Title');
            const description = parseField('Description');
            const maintainer = parseField('Maintainer').replace(/^@/, '');
            const discoverability = parseField('Discoverability Level') || 'Internal';
            const technicalDomain = parseCSV('Technical Domain Tags');
            const language = parseCSV('Language Tags');

            if (!title) {
              core.setFailed('Could not parse Lab Title from issue body');
              return;
            }

            // Generate kebab-case ID from title
            const id = title.toLowerCase()
              .replace(/[^a-z0-9\s-]/g, '')
              .replace(/\s+/g, '-')
              .replace(/-+/g, '-')
              .replace(/^-|-$/g, '');

            const today = new Date().toISOString().split('T')[0];

            const packet = {
              id: id,
              title: title,
              packetType: 'Hands On Lab',
              issueTemplate: 'propose-hands-on-lab.yml',
              description: description,
              maintainer: maintainer,
              created: today,
              updated: today,
              issueNumber: issue.number,
              publishState: 'Published',
              discoverability: discoverability,
              tags: {
                industry: [],
                productLine: ['Devin'],
                technicalDomain: technicalDomain,
                language: language,
                custom: ['hands-on-lab']
              },
              contentGroups: [],
              resources: {}
            };

            const fs = require('fs');
            const filename = `${id}.json`;
            const packetPath = `data/packets/${filename}`;

            // Write packet JSON
            fs.writeFileSync(packetPath, JSON.stringify(packet, null, 2) + '\n');

            // Update index.json
            const indexPath = 'data/packets/index.json';
            const index = JSON.parse(fs.readFileSync(indexPath, 'utf8'));
            if (!index.includes(filename)) {
              index.push(filename);
              fs.writeFileSync(indexPath, JSON.stringify(index, null, 2) + '\n');
            }

            // Export for later steps
            core.exportVariable('PACKET_FILENAME', filename);
            core.exportVariable('PACKET_TITLE', title);
            core.exportVariable('PACKET_ID', id);
            core.exportVariable('ISSUE_NUMBER', issue.number.toString());

      - name: Create branch and push
        run: |
          BRANCH="packet/${PACKET_ID}-from-issue-${ISSUE_NUMBER}"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -b "$BRANCH"
          git add data/packets/
          git commit -m "feat: add packet '${PACKET_TITLE}' from issue #${ISSUE_NUMBER}"
          git push origin "$BRANCH"
          echo "BRANCH=$BRANCH" >> "$GITHUB_ENV"

      - name: Open pull request
        uses: actions/github-script@v7
        with:
          script: |
            const pr = await github.rest.pulls.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `Add packet: ${process.env.PACKET_TITLE}`,
              head: process.env.BRANCH,
              base: 'main',
              body: `Adds a new Field Kit packet from issue #${process.env.ISSUE_NUMBER}.\n\nPacket file: \`data/packets/${process.env.PACKET_FILENAME}\`\n\nMerge this PR to publish the packet to the catalog.`
            });
            core.exportVariable('PR_NUMBER', pr.data.number.toString());
            core.exportVariable('PR_URL', pr.data.html_url);

      - name: Comment on issue
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: parseInt(process.env.ISSUE_NUMBER),
              body: `A pull request has been created to add **${process.env.PACKET_TITLE}** to the Field Kit catalog.\n\n**PR:** ${process.env.PR_URL}\n\nMerge the PR to publish this packet. You can also edit the packet JSON in the PR before merging.`
            });
